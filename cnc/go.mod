module nexus/cnc

go 1.21

// Zero external dependencies — pure Go standard library.
// The CNC speaks raw TCP, parses a binary bot protocol,
// and serves a telnet admin console. No frameworks needed.
